# Infinite Irrigation System - 更新日志

## Version 4.4.0 (Development)
**开发日期**: 2026-08-22
**主要更新**: 第一版无限供水与无限供电管道

### 🏗️ 垂直立管（开发素材，当前隐藏）
- 已保留一层楼高的北、东、南、西四向立管素材、注册贴图和存档兼容代码，便于后续继续调整美术
- 当前版本不在放置菜单和工坊说明中显示垂直立管，玩家无法新放置未完成造型
- 已经在开发存档中放置的立管仍可读取、切换模式和拆除，不会因为隐藏入口而损坏存档
- 保留现有贴图索引 `0–21` 不变，隐藏素材继续占用预留的 `22–29`，避免已有开发存档造型错位

### 🌐 语言支持
- 新增完整俄语本地化，覆盖物品、制作配方、沙盒设置、Mod 设置、放置菜单以及已放置管道的全部功能开关
- 补齐灌溉、自动翻土、供水、供电、范围选择、多人认领等近期功能的俄语文本
- 俄语文本根据当前版本的中英文原文独立翻译，不依赖外置翻译模组

### 🧵 制作便利性
- 无限管道改为可随时随地徒手制作，不再要求桌子、制作表面或工作台
- 制作不再受光照条件限制，也不需要任何工具
- 配方材料与产量保持不变：`1` 根碎布条制作 `16` 根无限管道

### 🚜 单管自动翻土
- 已放置管道的右键菜单新增“自动翻土”，可选择跟随全局、为此管道开启或为此管道关闭
- 已放置管道改用扁平状态菜单，依次显示灌溉、翻土、供电、供水、范围和移除；前四项可直接点击切换
- 自动翻土依赖灌溉覆盖：灌溉关闭时显示翻土已关闭，但原有偏好仍会保留
- 在仅供水或普通管道上点击开启翻土时，会一并恢复灌溉，避免菜单显示开启却不生效
- 右键命中已放置管道时不再重复显示放置管道与查看网络入口
- 老管道默认继续跟随全局设置，升级后不会改变现有世界的翻土行为
- 单管选择写入管道存档，并在灌溉、供水模式切换及重新载入存档后保留
- 重叠覆盖格只要被至少一根已开启翻土的管道覆盖就会处理，不会被另一根关闭的管道错误抑制
- 单管开关复用覆盖图与现有分批时间预算，不增加逐帧或额外全图扫描
- 多人模式由服务端校验玩家距离与选项值，客户端不能远程修改管道

### 🌱 可调灌溉范围
- 世界设置新增默认灌溉范围，可选 `1×1`、`3×3`、`5×5` 或 `7×7`，默认保持原来的 `3×3`
- 已放置管道的右键菜单提供当前“范围”，可选择跟随全局，或为该管道单独指定范围
- 老存档管道自动跟随全局默认值；单独范围会写入管道存档，并在灌溉、供水模式切换后继续保留
- 放置即时护理、播种与收获响应、长期覆盖扫描和自动翻土统一使用每根管道的有效范围
- 多人范围修改由服务端进行距离和值域校验，拒绝超过 `7×7` 的无效请求
- 大范围覆盖图继续按时间预算和工作数量分批重建，不增加逐帧扫描

### 🌾 收获经验与植物归属
- 新放置的管道会记录放置者，模式切换、范围调整及存档读取后继续保留归属
- 管道护理遇到没有 `owner` 的旧植物或异常植物时，会绑定到最近的有主管道并立即保存
- 已由原版播种行为登记主人的植物绝不覆盖，避免多人服务器中改变其他玩家的作物归属
- 单人旧管道自动迁移给本地玩家；多人无主管道会显示“认领管道”，只有玩家主动认领后才绑定
- 归属修复复用植物护理路径和坐标索引，不新增逐帧、逐分钟或全图扫描

### ⚡ 无限供电
- 修复 Build 42 已将原生区块边长改为 `8` 格，而供电登记仍按旧版 `10` 格计算，导致供电坐标写入错误区块、附近用电器仍显示没有电源的问题
- 修复 Build 42 会清除仅有坐标、没有原生供电对象的登记，导致菜单显示供电已开启但用电器实际无电的问题
- 管道新增独立“供电开关”，可与灌溉、供水任意组合；三项全部关闭时仍保留为普通地面管道
- 放置时新增“仅供电”和“全部功能”两个快捷模式，之后仍可通过右键菜单分别调整三项功能
- 供电直接接入 Build 42 原生发电机区块机制，用电器按原版方式识别电力
- 水平与垂直供电范围跟随世界的原版发电机沙盒设置，默认无需额外配置
- 不消耗燃料，不产生噪音或废气，也不会生成可见、可交互或需要维护的实体发电机
- 仅在放置、切换、区块载入时更新供电登记，并每十个游戏分钟低频校准一次；不逐格或逐帧扫描供电范围
- 内部原生供电凭证不保存、不联机传输，也不进入发电机更新列表；不会耗油、发声或执行逐帧维护
- 同一区块存在大量重叠供电管道时，会按楼层自动压缩为仍能保持完整覆盖的最小实用子集，并共享同样压缩后的内部供电凭证，避免原版用电查询逐根检查所有管道
- 多人模式由服务端校验开关操作并向客户端同步供电状态

### 🚰 管道工作模式
- 保持一种“无限灌溉管道”物品和原有制作配方，不再增加功能重复的独立物品
- 放置管道时可直接选择“仅灌溉”“仅供水”或“灌溉＋供水”作为初始模式，三种模式共用完整的直线、十字、拐角和三通造型
- 已放置的地面管道提供独立的“供水开关”和“灌溉开关”，支持仅灌溉、仅供水、两者同时开启以及两者同时关闭
- 开启供水后仍保留管道原有的方向和造型，不再统一显示为横向管道
- 两项功能都关闭时只保留普通地面管道，不进入植物护理网络，也不提供水源
- 开启供水后，管道可直接用于饮水、向容器装清水以及原版清洗操作
- 开启供水并放在用水器上一层的 `3×3` 范围内后，可作为水槽、洗衣机等原版用水器的外部水源
- 用水器仍需按照原版方式使用管钳连接
- 供水、饮水、装水和用水器查找全部使用 Build 42 原生流体机制，不增加定时扫描或补水任务
- 模式切换保留管道坐标和方向，并通过服务端距离校验避免多人远程修改
- 原版收获完成后立即按该植物坐标执行一次管道护理，复活或再生无需再等待十分钟轮询，同时不搜索周围植物或整个管网

## Version 4.3.3
**发布日期**: 2026-08-22
**主要更新**: 降低运行开销并增强管道生命周期一致性

### ⚡ 性能与兼容性
- 正式版关闭调试时立即跳过植物属性诊断，避免每轮护理仍进行字符串构造和属性读取
- 移除客户端启动及旧水桶检查遗留的调试输出
- 普通植物继续使用当前 Build 42 原版收获校验，仅为旧存档的 `harvest` 状态保留窄兼容分支
- 覆盖图在一次构建中同时生成查找表、确定顺序列表和总数，移除重复统计与未使用的管道列表
- 覆盖图重建后尽量从上次坐标继续轮询，避免管道变动时反复从起点扫描
- 新管道放置后直接护理自身九格范围，不再触发无关覆盖区的整批扫描
- 服务端使用坐标索引查找管道，大型存档载入管道时不再逐根扫描整张列表
- 覆盖记录直接保存轮询序号，移除与覆盖格数量相同的重复索引表
- 每分钟护理同时受批次数量和约 3 毫秒时间预算限制，大型农场或自动翻土遇到重操作时会保存游标并下轮继续
- 移除 1673 行未注册的旧管网、旧护理、抽样与性能测试代码，服务端主文件从 2727 行精简到 1054 行

### 🐛 管道状态修复
- 管道被大锤、管理员工具或其他模组移除时，服务端同步清理运行时与存档记录
- 读取存档后执行一次已加载区域校验，之后在区块载入时按坐标清理幽灵记录
- 未加载区块中的管道记录保持不变，避免误删远处的有效管道

## Version 4.3.2
**发布日期**: 2026-08-22
**主要更新**: 修复发布版运行边界并统一界面与文案

### 🐛 关键修复
- 不再覆盖全局 `print`，避免关闭原版及其他 Mod 的 Lua 日志
- 修复多人模式拆除管道时客户端与服务端命令名大小写不一致
- 修复多人服务器放置动作完成但管道没有生成的问题
- 修复“永不腐烂”仍会被原版成熟计时器腐烂的问题，并兼容迁移旧存档的 `harvest` 状态
- 服务端校验拆管坐标与玩家距离，拒绝无效或远程请求
- 拆除管道后立即重建覆盖图，旧区域不再继续获得护理

### 🖥️ 界面与发布一致性
- 网络界面移除已废弃的水桶状态，所有无限管道统一显示为正常工作
- 客户端管道列表支持添加去重与移除同步
- 配方恢复为 1 根碎布条制作 16 根管道
- 性能说明改为当前实际使用的自适应分批覆盖扫描

## Version 4.3.1
**发布日期**: TBD  
**主要更新**: 主要灌溉系统分批次处理

### 🔧 分批次处理完善
- 为主要灌溉系统添加分批次处理（每次处理20个管道）
- 新增 `buildBatchAdjMatrix()` 和 `buildBatchPlantsAdj()` 批次处理方法
- 实现管道轮询机制：`irrigationIndex` 记录处理进度
- 完善双重分批次系统：主要灌溉（20个管道/次）+ 即时灌溉（10个管道/次）

### ⚡ 性能提升效果
- 大型农场：避免一次性处理所有管道造成的卡顿
- 均匀负载：将处理压力分散到多个周期
- 完整覆盖：确保所有管道最终都会被处理
- 进度监控：清晰的批次处理日志

### 🎯 系统架构优化
- **主要灌溉**：每10分钟，批次处理20个管道，基于原版水分逻辑
- **即时灌溉**：每1分钟，批次处理10个管道，专注新种植物
- **智能采样**：随机采样检查，避免不必要的完整周期
- **冷却机制**：2小时冷却，减少空闲期开销

## Version 4.3.0n System - 更新日志

## Version 4.2.2
**发布日期**: TBD  
**主要更新**: 代码精简与架构优化

### � 代码优化
- 移除了所有不必要的barrel相关代码，简化系统架构
- 删除了 `buildWaterSources()` 和 `buildClustersSources()` 方法
- 删除了 `fusionClustersSharedSources()` 方法
- 简化了 `buildClusters()` 使用布尔值标记无限水源
- 精简了 `buildClusterWater()` 和 `waterPlants()` 方法
- 优化了 `updatePipes()` 主循环，移除barrel依赖

### 🏗️ 架构改进
- 无限水源系统现在使用更简洁的 -1 标记方案
- 减少了代码复杂度，提高了可维护性
- 保持了所有核心功能：无限灌溉、施肥、性能优化

## Version 4.2.1
**发布日期**: 2024-12-19  
**主要更新**: 调试输出国际化

### 🌍 国际化改进
- 所有调试输出改为英文，便于国际用户理解
- 保留中文注释便于开发维护
- 统一调试信息格式：`[Smart Irrigation]`, `[Infinite Fertilizer]`, `[Plant Detection]`

### 🐛 代码质量
- 标准化调试输出语言
- 改进错误信息可读性
- 保持功能完整性的同时提升用户体验

## Version 4.2.0

---

## v4.1.1 - 2025年8月14日 ✅ 稳定版本
**关键Bug修复版本**

### 🐛 Bug修复：
- 修复null对象访问错误，防止游戏崩溃
- 解决灌溉过程中的空指针异常
- 提升系统稳定性和容错能力

### 🌟 核心功能：
- ✅ 无限水源：永不缺水的智能灌溉
- ✅ 3x3范围：每根管道覆盖3x3区域  
- ✅ 智能适配：根据作物类型自动调节水分
- ✅ 一次到位：直接补水到最佳水分，无需等待
- ✅ 全作物兼容：支持原版及模组作物
- ✅ 零维护：无需连接水源、无需材料、无需建筑

### 🔧 技术实现：
- 智能检测植物类型和最大水分需求
- 自动获取作物的waterLvlMax配置
- 兼容farming_vegetableconf.props系统
- 提供详细的调试日志输出

### 📋 使用方法：
1. 制作"Irrigation Pipe"（无需材料）
2. 放置在农田中心
3. 周围3x3范围内的作物自动获得最佳水分
4. 完全自动化，无需任何维护

### 🎯 适用版本：
- Project Zomboid Build 42+
- 与所有农业模组兼容

---

## 开发历史：

### v4.1 - 智能最大水分适配系统
- 添加动态水分检测
- 实现作物特异性灌溉

### v4.0 - 完美智能灌溉系统
- 重构核心灌溉逻辑
- 实现一次到位补水策略

### v3.x及以前
- 基础3x3灌溉功能
- 原始固定水分系统
